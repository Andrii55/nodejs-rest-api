const authController = require("./auth.controller");
const contactsController = require("./contacts");
const imageController = require("./updateImage");
const verifyController = require("./verification");

module.exports = {
  authController,
  contactsController,
  imageController,
  verifyController,
};
